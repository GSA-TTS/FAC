# used by cloud.gov
web: echo 'Starting procfile....' &&
    python -m pip install -r requirements-nohashes.txt &&
    python manage.py migrate &&
    python manage.py collectstatic --noinput &&
    gunicorn config.wsgi -t 60
