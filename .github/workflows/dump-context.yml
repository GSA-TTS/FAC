name: dump

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  dump:
    runs-on: ubuntu-latest
    steps:
      - name: Dump context
        uses: crazy-max/ghaction-dump-context@v2
